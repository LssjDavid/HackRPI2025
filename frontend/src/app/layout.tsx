import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: {
    default: 'CSV Data Analyzer',
    template: '%s | CSV Analyzer',
  },
  description: 'Automated CSV analysis and visualization tool for data engineers',
  keywords: ['CSV', 'data analysis', 'visualization', 'data engineering'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
